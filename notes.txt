If we want export multiple parts of the same file, we add the name `export` to each variable and function we want to export.

Then we can import them using the same syntax as before. Only this time we separate the parts with a comma. `import { functionName, variableName } from './${filePath}'`

video #7