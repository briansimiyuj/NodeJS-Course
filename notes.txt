To use Express:

    1. import express
    2. create an instance of express and assign it to a variable
    3. listen on a port through the variable
    4. create a route
        i. use http methhods on the variable
        ii. use a callback function 
        
video #23