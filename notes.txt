node-dev is a package that allows us to restart NodeJS server every time we make a change to our code.
To install node-dev, we run:

    `npm i -g node-dev` for global installation or `npm i node-dev --save-dev` for local installation

To run our server, we run:

    `node-dev ${fileName}.js`
   
video #22