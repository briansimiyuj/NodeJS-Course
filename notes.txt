Now let's see how we can serve HTML files using writable streams.

Create index.html file and add some markup to it.

Then do as we did in the previous commit but this time, we are serving the index.html file.

video #17