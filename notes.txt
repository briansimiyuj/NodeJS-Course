When we run JavaScript in the browser,the global object is called window.

But in Node.js, the global object is called global.

Some of methods and properties are in window object are in global object, like setTimeout, clearTimeout, setInterval, clearInterval, etc.

video #4