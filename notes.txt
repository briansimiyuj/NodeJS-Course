Let's create a file called script.js and write some code in it.

To run the file, we run the following command:

    `node ${fileName}.js` or `node ${fileName}`

NB: You need to be in the directory where the file is located.

video #2