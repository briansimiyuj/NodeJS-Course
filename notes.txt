In the last commit, we sent responses with strings to dynamic routes. Let's now send HTML files.
To do this, we'll use the sendFile($path) method. $path has two parts: $__currentDirectory and $fileName.

video #25